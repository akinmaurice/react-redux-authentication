import React from 'react';
import Jumbotron from './utils/Jumbotron';
import Header from './Header';

const Home = props => (
  <div>
    <Header />
    <Jumbotron />
  </div>
);
export default Home;
